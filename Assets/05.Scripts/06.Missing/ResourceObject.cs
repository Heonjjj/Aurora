using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ResourceObject : MonoBehaviour
{
    //public ItemData itemToGive;   // 채집 시 드랍될 아이템 정보
    //public int quantityPerHit = 1;  // 한 번 Gather 시 몇 개 드랍할지
    //public int capacy; //총 체력?   // 오브젝트의 총 체력/내구도
    //
    //public void Gather(Vector3 hitPoint, Vector3 hitNormal)
    //{
    //    for (int i = 0; i < quantityPerHit; i++)
    //    {
    //        if (capacy <= 0) break;  // 체력이 다 되면 중단
    //        capacy -= 1;
    //        Instantiate(itemToGive.dropPrefab, hitPoint + Vector3.up
    //        ,Quaternion.LookRotation(hitNormal, Vector3.up));
    //    }   // 플레이어 위치 위쪽에 생성
    //}
}


//채집 자원에 대한 정보